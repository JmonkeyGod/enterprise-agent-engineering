---
import DocsLayout from '../../layouts/DocsLayout.astro'; import Hero from '../../components/Hero.astro'; import Takeaway from '../../components/Takeaway.astro';
const base=import.meta.env.BASE_URL; const href=(v:string)=>`${base}${v}`.replace(/\/+/g,'/');
---
<DocsLayout title="Recipes" section="Recipes"><Hero eyebrow="Technology-specific" title="Recipes" lede="Recipes show how current tools implement EADL concepts. They are useful, replaceable, and intentionally secondary." />
<section class="block"><div class="cards"><a class="card" href={href('recipes/langgraph-explicit-state/')}><small>State</small><h3>LangGraph for Explicit State</h3><p>Typed state, conditional routes, checkpointing, interrupts, and recovery.</p></a></div></section>
<Takeaway>Choose a framework because it serves the design—not because it names the design.</Takeaway></DocsLayout>
