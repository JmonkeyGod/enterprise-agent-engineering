---
import DocsLayout from '../../layouts/DocsLayout.astro'; import Hero from '../../components/Hero.astro'; import Takeaway from '../../components/Takeaway.astro';
const base=import.meta.env.BASE_URL; const href=(v:string)=>`${base}${v}`.replace(/\/+/g,'/');
---
<DocsLayout title="Patterns" section="Patterns"><Hero eyebrow="Reusable structures" title="Patterns" lede="Patterns combine stable concepts into repeatable enterprise structures. They describe design, not a particular SDK." />
<section class="block"><div class="cards">
<a class="card" href={href('patterns/workflow-agent/')}><small>Control</small><h3>Workflow + Agent</h3><p>A deterministic skeleton with one or more bounded decision points.</p></a>
<a class="card" href={href('patterns/human-in-control/')}><small>Safety</small><h3>Human in Control</h3><p>Pause, explain impact, authorize, resume, and audit.</p></a>
</div></section><Takeaway>A pattern earns its place by removing repeated design uncertainty.</Takeaway></DocsLayout>
